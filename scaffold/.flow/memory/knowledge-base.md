# Debug Knowledge Base

> **Append only. Never rewrite. Never delete.**
> The debug agent reads this before every investigation.
> Cold-start elimination — known issues are resolved immediately.

Format for every entry:
```
## [Error / Symptom] — YYYY-MM-DD
**Stack / Runtime:** [language, framework, version if relevant]
**Symptom:** what the error looked like
**Root Cause:** what actually caused it
**Fix:** exact steps or code that resolved it
**Recurrence prevention:** what to check in future
```

---

*No entries yet. Entries are added automatically when `/flow-verify-work` resolves an issue.*
