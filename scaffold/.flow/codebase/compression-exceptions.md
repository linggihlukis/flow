# Compression Exceptions

Zones that are always included in PATTERNS.md scoped extracts, regardless of the phase's zone scope. Added automatically by the ACON compression feedback loop (AGENTS.md §19) when the debugger detects compression loss.

_(Empty — zones are added automatically as the debugger identifies compression loss.)_
